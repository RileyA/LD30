/**
 * Ensure the user-agent supports all the stuff we'll be using.
 * Returns the empty string if we're all good. Returns an error
 * string otherwise.
 */
function check_ua() {
  // TODO: all of this.
  return '';
}
